/**
 * @file setup.ts
 * @description Configuración global para tests E2E
 */

// Aumentar timeout para tests E2E
jest.setTimeout(30000);

// Silenciar logs durante tests (opcional)
// console.log = jest.fn();
// console.warn = jest.fn();
